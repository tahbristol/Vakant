<div class="col-md-12">
  <div>
    <% @jobs.each.with_index(1) do |job,i| %>
      <%= link_to "#{i}. #{job.title}", job_path(job) %><br>
      <%= job.location %><br>
      <%= truncate job.description %><br>
      <%= job.level %><br>
      <%= job.salary %><br>
    <%end%>
  </div>
</div>
